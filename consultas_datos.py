
#------------------------------------------

usuarios = filter(lambda usuario:usuario.id == id , usuario_lista) 

lambda funcion anonima


try:
     list(usuarios)[0] 
except:
   return {
     'error':' usuario no encontrado '
   }

filter(funcion,lista)
filter solo coje cada elemento de la lista , donde cada elemento se la aplica una funcion donde la condicion sea verdadera.

lambda  lambda argumentos: expresion 
funciones anonimas 

lambda argumentos : expresion   = funcion .  
filter(funcion,lista) 



